specific_file_ini_config = {
{%- for item in config.app_test.app_config.ini_config.specific_file %}
    '{{ item }}': [{{ config.app_test.app_config.ini_config.specific_file[item] }}]
    {{- "," if not loop.last }}
{%- endfor %}
}